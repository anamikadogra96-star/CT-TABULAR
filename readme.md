# CT-TABULAR 📊

## 📌 Project Overview

**CT-TABULAR** is a web-based Python project that takes input data from a form and predicts student performance using a pre-trained model.  
This project uses machine learning to provide predictions based on tabular data for students. :contentReference[oaicite:1]{index=1}

---

## 🚀 Features

✔ User friendly HTML interface to input features  
✔ Python backend (Flask) to process data via a model  
✔ Outputs prediction results in a styled result page  
✔ Uses a machine learning model inside `model.py` for inference :contentReference[oaicite:2]{index=2}

---

## 🛠️ Technologies Used

- Python  
- Flask (Backend web framework)  
- HTML / CSS (Frontend interface)  
- Machine Learning Model (in `model.py`)  
- CSV dataset for training input (`StudentsPerformance.csv`) :contentReference[oaicite:3]{index=3}

---

## 📂 Repository Structure

